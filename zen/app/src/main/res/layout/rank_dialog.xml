<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/text_margin_big">

    <TextView
        android:id="@+id/rank_dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:paddingBottom="@dimen/text_margin_big"
        android:text="@string/rank_dialog_title"
        android:textAlignment="center"
        android:textSize="20sp"/>

    <RatingBar
        android:id="@+id/rank_dialog_ratingbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:clickable="true"
        android:isIndicator="false"
        android:numStars="@integer/stars_amount"
        android:rating="2"
        android:stepSize="1.0"
        android:theme="@style/RatingBar"/>

</LinearLayout>